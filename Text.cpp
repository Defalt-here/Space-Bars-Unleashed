#include "Text.h"

Text::Text()
{
    //ctor
    Message_rect.x = 225;  //controls the rect's x coordinate
    Message_rect.y = 250; // controls the rect's y coordinte
    Message_rect.h = 100; // controls the height of the rect
    Message_rect.w = Message_rect.h*4; // controls the width of the rect
}
points_text :: Points_Text(){
    Points_rect.x = 0;  //controls the rect's x coordinate
    Points_rect.y = 0; // controls the rect's y coordinte
    Points_rect.h = 100; // controls the height of the rect
    Points_rect.w = Points_rect.h*4; // controls the width of the rect
}
